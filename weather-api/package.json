{
  "name": "weather-api",
  "scripts": {
    "install:all": "cd api && bun install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "dev": "concurrently \"cd api && bun run dev\" \"cd frontend && npm run dev\"",
    "start": "concurrently \"cd api && bun run start\" \"cd frontend && npm run preview\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
